print("> python boxes.py")
number_of_items = int(input("Enter the number of items: "))
items_per_box = int(input("Enter the number of items per box: "))

import math
needed_boxs = math.ceil(number_of_items/ items_per_box)

print(f"\nFor {number_of_items} items, packing {items_per_box} items in each box, you will need {needed_boxs} boxes.")

# Run again as the prepare web

print("\n> python boxes.py")
number_of_items = int(input("Enter the number of items: "))
items_per_box = int(input("Enter the number of items per box: "))

import math
needed_boxs = math.ceil(number_of_items/ items_per_box)

print(f"\nFor {number_of_items} items, packing {items_per_box} items in each box, you will need {needed_boxs} boxes.")
