names = []
numbers = []

for i in range(len(names)):
    names = names[i]
    numbers =numbers[i]
    #把names 變成i for names
    print(f'{names} - {numbers}')


#REMOVING ITEMS FROM A LIST
#使用'pop'
the_list.pop(3) #Removes the item at index 3
the_list.pop()# Removes the last item